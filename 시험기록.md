# 제비 잘림 — 실측 기록지 (육안 관찰용)

로그와 **별개로** 눈으로 본 결과를 남깁니다. `D unreadable` 이 나오면
이 기록이 유일한 판단 근거가 됩니다.

## 데이터 오염 방지 규칙 (4가지 — 지키지 않으면 결과 무효)

1. **P1은 파일로 고정** — `payload-P1.txt` 를 만들어 두고 **9회 전부 같은
   파일에서** 복사합니다. 매번 다시 타이핑·재편집 금지.
   `make-payloads.mjs` 도 이 파일을 기준으로 P2·P3 길이를 맞춥니다.
2. **시행 사이 응답 완료 대기** — 세 창 모두 답변 스트리밍이 **끝난 뒤**
   다음 시행. 이전 응답의 CPU 부하가 경합 측정을 오염시킵니다.
3. **대화 누적 조건 고정** — **매 시행 새 대화**(명령바 `새 대화` 버튼).
   대화가 길어지면 DOM이 무거워져 조건이 달라집니다.
4. **다른 무거운 프로그램 종료** — 경합을 재는 중이라 백그라운드 부하는 노이즈.

## 환경

- 날짜:
- 기기 (맥미니 27" / 노트북 14"):
- P1 글자 수 / 개행 수:  (`node tools/analyze-logs.mjs` 출력의 A 줄과 대조)
- 배치 모드 (3분할 / 겹침):

## 육안 확인은 반드시 **두 곳**에서

"잘림 O/X"를 적을 때 **입력창**과 **전송된 버블**을 따로 보십시오.
이 둘이 갈리면 그 자체가 판정입니다 — 로그 나오기 전에 1·2순위가 갈립니다.

| 관찰 | 뜻 |
|---|---|
| 입력창부터 짧다 | **주입 실패** (C < B) — 1순위 가설 |
| 입력창은 멀쩡한데 버블만 짧다 | **조기 전송** (D < C) — 2순위 가설 |
| 둘 다 멀쩡 | 잘림 없음 — 표시 문제나 재현 실패 |

기록 형식:
```
제비-입력창  : 전문 O / 앞부분만 X
제비-전송버블: 전문 O / 앞부분만 X
```

## 2단계 — P1 · 3창 · 3회 (기저 실패율)

> 0/3 실패면 **재현 실패**입니다. payload를 더 길게/개행을 더 많이 해서 다시 잡으십시오.

```
P1-3창-1회  제비 입력창 ? / 버블 ?   참새 입력창 ? / 버블 ?   하마 입력창 ? / 버블 ?
P1-3창-2회  제비 입력창 ? / 버블 ?   참새 입력창 ? / 버블 ?   하마 입력창 ? / 버블 ?
P1-3창-3회  제비 입력창 ? / 버블 ?   참새 입력창 ? / 버블 ?   하마 입력창 ? / 버블 ?
```

## 3단계 — P1 · 단독(제비만) · 3회

> **2단계와 같은 payload 파일이어야 합니다.** 설정 → 단독 실행 대조 → 제비만.

```
P1-단독-1회  제비 입력창 ? / 버블 ?
P1-단독-2회  제비 입력창 ? / 버블 ?
P1-단독-3회  제비 입력창 ? / 버블 ?
```

→ 끝나면 설정을 **"세 곳 모두"로 되돌리기**

## 4단계 — P2 · 3창 · 3회 (개행 5개)

```
P2-3창-1회  제비 입력창 ? / 버블 ?   참새 입력창 ? / 버블 ?   하마 입력창 ? / 버블 ?
P2-3창-2회  제비 입력창 ? / 버블 ?   참새 입력창 ? / 버블 ?   하마 입력창 ? / 버블 ?
P2-3창-3회  제비 입력창 ? / 버블 ?   참새 입력창 ? / 버블 ?   하마 입력창 ? / 버블 ?
```

## 5단계 — P3 · 3창 · 3회 (개행 수백 개)

```
P3-3창-1회  제비 입력창 ? / 버블 ?   참새 입력창 ? / 버블 ?   하마 입력창 ? / 버블 ?
P3-3창-2회  제비 입력창 ? / 버블 ?   참새 입력창 ? / 버블 ?   하마 입력창 ? / 버블 ?
P3-3창-3회  제비 입력창 ? / 버블 ?   참새 입력창 ? / 버블 ?   하마 입력창 ? / 버블 ?
```

## 2단계가 0/3 실패일 때 — P1 키우기 (한 번에 하나씩)

재현이 안 되면 여기서 멈추지 말고 **한 변수씩** 키우십시오.
재현되는 순간 멈추고 그 파일을 **새 P1** 으로 삼습니다.
마지막에 바꾼 변수가 곧 **임계 변수**입니다.

```
node tools/grow-payload.mjs 길이   → payload-P1-길이2배.txt   (개행 비율 유지)
node tools/grow-payload.mjs 개행   → payload-P1-개행2배.txt   (길이 유지)
node tools/grow-payload.mjs 구조   → payload-P1-구조2배.txt   (표·코드펜스 밀도)
```

| 순서 | 바꾼 것 | 3창 3회 결과 | 재현? |
|---|---|---|---|
| 1 | 길이 2배 |  |  |
| 2 | 개행 2배 |  |  |
| 3 | 구조 2배 |  |  |

셋 다 재현 실패면 **재현 조건이 다른 데 있다는 뜻**입니다. 그 상태 그대로
가져오십시오 — 접근을 다시 짜야 합니다.

> 참고: `구조` 모드는 표·코드펜스 블록을 덧붙이므로 길이도 함께 늘어납니다
> (순수 단일 변수가 아님). 1·2번에서 재현되지 않은 뒤에만 쓰십시오.

## 6단계 — 로그

- 명령바 `진단 로그` 버튼 → `3ai-diag-logs.json` 저장
- `node tools/analyze-logs.mjs ~/Downloads/3ai-diag-logs.json`
- 출력 전체 + 이 기록지를 함께 전달

## 특이사항 메모

(예: 어느 시행에서 제비 창이 스크롤 튀었다, 말풍선이 접혀 있었다 등)
