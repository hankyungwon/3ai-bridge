# 수리요청.md — 확장이 깨졌을 때 붙여넣는 수리 프롬프트

Claude·ChatGPT·Gemini는 화면 구조를 자주 바꿉니다. 그러면 확장이 입력란이나
전송 버튼을 못 찾아서 팝업에 ❌ 가 뜹니다. **이때 고쳐야 할 파일은 `selectors.js`
단 하나입니다.**

## 1단계 — 어디가 깨졌는지 확인

팝업에 전송해 보고 표시된 문구를 그대로 메모합니다. 예:

```
✅ Claude — 전송됨
✅ ChatGPT — 전송됨
❌ Gemini — 입력란을 찾지 못함 (selectors.js 갱신 필요)
```

## 2단계 — 아래 프롬프트를 복사해 클로드 코드에 붙여넣기

`○○` 부분만 실제 사이트 이름(Claude / ChatGPT / Gemini)으로 바꾸세요.

---

> 이 저장소는 3-AI 동시 질문 크롬 확장(3대장 브리지)이다.
> `selectors.js`의 **○○** 사이트 선택자가 깨졌다. 현재 페이지 구조를 확인해 갱신하라.
>
> 증상: 팝업에 `❌ ○○ — (여기에 표시된 문구 그대로 붙여넣기)` 가 표시된다.
>
> 요구사항:
> 1. `selectors.js`의 해당 사이트 항목에서 `입력란`, `전송버튼`(필요하면 `모델버튼`,
>    `모델항목`) 후보 배열을 갱신하라.
> 2. **기존 선택자를 지우지 말고 새 선택자를 배열 맨 앞에 추가**하라.
>    (배열은 위에서부터 시도하므로 예전 구조도 계속 지원된다.)
> 3. 클래스 이름처럼 자주 바뀌는 값보다 `data-testid`, `aria-label`,
>    `contenteditable` 같은 안정적인 속성을 우선 사용하라.
> 4. `selectors.js` 외의 파일은 되도록 수정하지 마라.
> 5. 수정 후 무엇을 왜 바꿨는지 한국어로 요약하라.

---

## 3단계 — 직접 선택자를 찾아 알려주는 방법 (선택)

클로드 코드가 사이트 화면을 직접 볼 수 없을 때는, 아래 방법으로 정보를 떠서
위 프롬프트에 함께 붙여넣으면 훨씬 정확해집니다.

1. 문제가 되는 사이트를 크롬에서 엽니다.
2. 입력란 위에서 **마우스 오른쪽 클릭 → 검사(Inspect)** 를 누릅니다.
3. 파랗게 표시된 줄 위에서 오른쪽 클릭 → **Copy → Copy outerHTML**.
4. 붙여넣은 뒤, 전송 버튼에 대해서도 같은 방법으로 한 번 더 합니다.

## 참고 — 자주 나오는 실패 문구와 뜻

| 팝업 문구 | 뜻 | 조치 |
|---|---|---|
| 입력란을 찾지 못함 | `입력란` 선택자가 낡음 | 위 프롬프트 사용 |
| 입력란에 글자를 넣지 못함 | 입력 방식이 바뀜 | 위 프롬프트 사용 |
| 창이 열려 있지 않음 | 해당 사이트 창이 없음 | Alt+3 으로 창 열기 |
| 페이지와 통신 실패 | 확장 설치 전부터 열려 있던 탭 | 해당 탭 새로고침(F5) |
| 모델 자동 선택 실패 (모델 버튼 없음) | `모델버튼` 선택자가 낡음 | 아래 모델 전용 프롬프트 사용 |
| 모델 자동 선택 실패 (목록에 해당 모델 없음) | 모델 표기가 바뀌었거나 이름이 다름 | `selectors.js`의 `모델별칭`에 표기 추가 |
| 모델 자동 선택 실패 (선택 확인 실패) | `모델라벨` 선택자가 낡음 | 아래 모델 전용 프롬프트 사용 |

## 모델 자동 선택만 깨졌을 때 쓰는 프롬프트

> 이 저장소는 3-AI 동시 질문 크롬 확장(3대장 브리지)이다.
> **○○** 사이트의 **모델 자동 선택**이 실패한다. 팝업 문구는
> `ℹ ○○ 모델 자동 선택 실패 (여기에 괄호 안 사유 그대로)` 이다.
>
> `selectors.js`에서 해당 사이트의 `모델버튼`, `모델항목`, `모델라벨`,
> `더보기문구` 를 현재 페이지 구조에 맞게 갱신하고, 원하는 모델의 화면 표기가
> 바뀌었다면 파일 아래쪽 `모델별칭` 표에도 표기를 추가하라.
> 기존 값은 지우지 말고 새 값을 배열 맨 앞에 추가하라.
> 모델 선택은 실패해도 전송을 막지 않는 "베스트 에포트" 원칙을 반드시 유지하라.
