# 설치법.md — 비개발자용 설치 안내 (윈도우 노트북 / 맥 공통)

> 프로그램 이름: **AI 3대장(제비·참새·하마) 카페** — 제비=Gemini, 참새=ChatGPT, 하마=Claude

전문 지식 없이 10분이면 끝납니다. 크롬 브라우저만 있으면 됩니다.

---

## 0. 준비물

- 크롬 브라우저
- Claude, ChatGPT, Gemini에 **미리 로그인**되어 있을 것
  (이 확장은 이미 로그인된 창을 그대로 씁니다. 아이디/비밀번호를 묻지 않습니다.)

---

## 1. 파일 내려받기

1. 이 저장소 웹페이지에서 초록색 **`Code`** 버튼 → **`Download ZIP`** 을 누릅니다.
2. 내려받은 ZIP 파일을 **압축 해제**합니다.
   - 윈도우: 파일 오른쪽 클릭 → "압축 풀기"
   - 맥: 파일 더블클릭
3. 압축을 푼 폴더를 **앞으로 지우지 않을 곳**에 둡니다.
   (예: 윈도우 `문서\3ai-bridge`, 맥 `서류\3ai-bridge`)
   ※ 이 폴더를 지우거나 옮기면 확장이 작동을 멈춥니다.

폴더 안에 `manifest.json` 파일이 보이면 제대로 된 폴더입니다.

---

## 2. 크롬에 설치하기

1. 크롬 주소창에 다음을 입력하고 Enter:
   ```
   chrome://extensions
   ```
2. 화면 **오른쪽 위 "개발자 모드"** 스위치를 켭니다.
3. 왼쪽 위에 나타난 **"압축해제된 확장 프로그램을 로드합니다"** 버튼을 누릅니다.
4. 1단계에서 압축을 푼 **폴더를 선택**합니다.
   (폴더 안으로 들어가지 말고, `manifest.json` 이 들어 있는 그 폴더를 선택)
5. "3대장 브리지" 카드가 목록에 나타나면 설치 완료입니다.

### 아이콘을 항상 보이게 하기 (권장)

주소창 오른쪽 **퍼즐 조각 모양(🧩)** 을 누르고, "3대장 브리지" 옆의
**압정(📌) 아이콘**을 눌러 고정합니다.

---

## 3. 사용법

### ① 창 3개 한 번에 열기

키보드에서 **`Alt` + `3`** 을 누릅니다. (맥에서도 `Alt`(=`Option`) + `3`)

왼쪽부터 **Gemini · ChatGPT · Claude** 순서로 창이 열리면서 화면이 가로로
3등분되어 정렬됩니다. (순서는 ⚙ 설정에서 바꿀 수 있습니다.)
이미 열려 있는 창이 있으면 새로 열지 않고 위치만 다시 잡아 주고,
사이트가 다른 탭들과 한 창에 섞여 있으면 그 탭만 살짝 떼어내
전용 창으로 만들어 줍니다. (진행 중이던 대화는 그대로 유지됩니다.)

- **모니터가 여러 대**라면, 지금 쓰고 있는 크롬 창이 있는 모니터에 펼쳐집니다.
- **넓은 화면(27인치 등)**: 가로 3등분으로 나란히 배치됩니다.
- **좁은 화면(노트북)**: 자동으로 **겹침 배치**가 됩니다. 각 창이 화면의
  절반 폭을 차지해 잘 보이고, 가운데 창이 양옆 창을 반씩 가리며 맨 앞에
  옵니다. **보고 싶은 창에 마우스를 올리고 잠깐(0.4초) 머물면 그 창이
  앞으로 나옵니다.** (클릭해도 됩니다)
- 배치 방식은 ⚙ 설정에서 "자동 / 항상 3분할 / 항상 겹침"으로 바꿀 수 있습니다.

> 단축키가 다른 프로그램과 겹칠 때: 주소창에 `chrome://extensions/shortcuts` 를
> 입력하면 원하는 키로 바꿀 수 있습니다.

### ② 질문 한 번 입력해서 3곳에 동시 전송

1. `Alt`+`3`을 누르면(또는 툴바 아이콘 클릭) **화면 맨 아래에 가로로 긴
   명령바**가 나타납니다. AI 창들은 명령바 높이만큼 짧게 배치되므로
   명령바가 화면을 가리지 않습니다.
   - 명령바는 **다른 창을 클릭해도 닫히지 않습니다.**
   - 명령바의 **[Gemini] [ChatGPT] [Claude] 버튼**으로 가려진 창을 한 번에
     앞으로 가져올 수 있습니다. 단축키도 있습니다:
     **`Alt`+`1`(왼쪽) / `Alt`+`2`(가운데) / `Alt`+`4`(오른쪽)**
     — 겹침 배치에서 가운데 창이 완전히 가려졌을 때 `Alt`+`2`가 특히 유용
   - **🕘 버튼**을 누르면 명령바가 위로 펼쳐지며 질문 이력과 모은 답변이
     보입니다. 다시 누르면 접힙니다.
2. 명령바는 세 AI 창(밝은 화면)과 구별되는 **어두운 지휘소 디자인**입니다.
3. 질문을 입력하고 **`Enter`** 를 누르면 세 곳에 동시 전송됩니다.
   (줄바꿈이 필요하면 `Shift`+`Enter`)
3-1. **파일·사진도 함께 보낼 수 있습니다** (총 15MB까지):
   - **＋첨부** 버튼으로 파일 선택, 또는
   - 입력칸에 **붙여넣기(Ctrl+V)** — 캡처한 이미지 등, 또는
   - 파일을 입력칸에 **끌어다 놓기**
   담긴 파일은 입력칸 위에 📎칩으로 표시되고 ×로 뺄 수 있습니다.
   전송하면 세 사이트에 "붙여넣은 것처럼" 함께 들어갑니다.
   (사이트별 지원 형식이 다르므로, 일부 사이트는 특정 파일을
   받지 않을 수 있습니다 — 그 경우 글만 전송되고 알림이 뜹니다)
4. 명령바 오른쪽 끝에는 **최근 질문 목록**이 시각("3시간 전" 등)과 함께
   보입니다. 클릭하면 입력칸으로 다시 불러옵니다.
5. 프로필(표준/심층/신속)과 접두어는 **⚙ 설정**에서 관리합니다.
   각 AI의 모델·사고 수준은 각 AI 창에서 직접 고르면 그대로 유지됩니다.

### ③ 명령창의 리모컨 기능

- **답변 진행 표시**: 전송 후 명령창에 `✍️ChatGPT · ✅Gemini` 처럼 사이트별
  진행 상황이 실시간으로 뜹니다. 창을 들여다보지 않아도 어디가 끝났는지
  알 수 있습니다. — 모두 완료되면 "모두 완료!" 표시
- **📋 세 답변 모으기**: 버튼 하나로 세 사이트의 최신 답변을 걷어 와
  클립보드에 복사하고 명령창에서 펼쳐볼 수 있습니다. 모은 내용을 다시
  한 AI에게 보내 "세 답변을 교차 검증하라"고 시키는 것도 가능합니다.
- **🆕 새 대화**: 세 사이트를 한 번에 새 대화 화면으로 이동
- **❌ 모두 닫기**: 세 AI 창을 한 번에 닫기 (대화 기록은 각 사이트에 남음)
- **🪟 정렬**: 창이 흐트러졌을 때 다시 배치

### ③-1 질문 이력 관리

- **전송하면 입력칸이 비워지고**, 질문은 아래 이력에 쌓입니다
  (최근 500개, 내 컴퓨터에만 저장. 크롬을 껐다 켜도 유지)
- **날짜별 묶음**: 오늘 / 어제 / 그 이전 날짜로 나뉘어, 어제·그제 무슨
  질문을 했는지 맥락을 바로 되짚을 수 있습니다
- **☆ 별표**: 자주 쓰는 질문을 즐겨찾기로 맨 위에 고정 (질문 템플릿처럼 사용)
- **🔍 검색**: 지난 이력에서 키워드로 바로 찾기
- **내보내기**: 이력 전체를 파일(.md)로 저장 — 확장을 지워도 남는 백업
- 클릭 한 번으로 입력칸에 다시 불러와 수정·재전송

### ④ 이어서 질문하기

같은 방법으로 다시 전송하면 됩니다. 창을 닫지 않는 한 **각 사이트의 진행 중인
대화에 이어서** 전송됩니다.

### ④ 결과 확인

팝업 아래쪽에 사이트별 상태가 표시됩니다.

```
✅ Claude — 전송됨
✅ ChatGPT — 전송됨
❌ Gemini — 입력란을 찾지 못함
```

한 곳이 실패해도 **나머지 두 곳은 정상적으로 전송됩니다.**
전송 도중 팝업이 닫혔더라도, 10분 안에 팝업을 다시 열면
"지난 전송 결과"가 표시됩니다.

---

## 4. 설정 바꾸기

팝업 맨 아래 **⚙ 설정** 을 누르면:

- 창 배치 순서 (왼쪽 → 오른쪽)
- 프로필의 이름과 접두어 문구 (자유롭게 수정·추가·삭제)
- 사이트별로 다른 접두어
- 프로필별 **희망 모델명** (예: Claude=`Fable`, ChatGPT=`최상위`, Gemini=`Pro`)

### 모델 이름은 어떻게 적나요?

화면에 보이는 대로 **일부만** 적어도 됩니다. 대소문자·띄어쓰기는 무시합니다.

- `Fable`, `Opus`, `Sonnet`, `Pro`, `Flash`, `Thinking` …
- `최상위` 라고 적으면 그 사이트의 상위 모델(Claude=Fable/Opus,
  ChatGPT=Pro/Thinking, Gemini=Pro 등)을 알아서 찾아 줍니다.

목록에 바로 안 보이는 모델은 "더 보기 / 레거시 모델" 메뉴까지 펼쳐서 찾고,
고른 뒤에는 버튼 글자를 다시 읽어 **정말 바뀌었는지 확인**합니다.
이미 원하는 모델이 선택돼 있으면 아무것도 건드리지 않습니다.

> 모델 자동 선택은 "되면 좋고" 방식입니다. 실패해도 전송은 그대로 진행되고,
> 각 사이트가 기억하는 마지막 모델이 사용됩니다. 실패 시 팝업에 이유가
> 함께 표시됩니다 (예: `목록에 해당 모델 없음`).

---

## 4-1. 맥(Mac) 설치 — 터미널 한 줄이 가장 확실합니다

맥에서 `업데이트.command` 를 더블클릭하면 보안 경고가 뜰 수 있습니다:

> "'업데이트.command'을(를) 열지 않음 — 악성 코드가 없음을 확인할 수 없습니다"

**이때 절대 "휴지통으로 이동"을 누르지 마세요. "완료"를 누르면 됩니다.**
인터넷에서 받은 스크립트에 맥이 붙이는 표준 경고일 뿐입니다.

### 방법 A — 터미널 한 줄 (권장, 30초)

1. `Command`(⌘) + `스페이스` → **터미널** 입력 → Enter
2. 아래 한 줄을 통째로 복사해 붙여넣고 Enter:

```
mkdir -p ~/Documents/3ai-bridge && cd ~/Documents/3ai-bridge && curl -sSL -o main.zip https://github.com/hankyungwon/3ai-bridge/archive/refs/heads/main.zip && ditto -x -k main.zip . && cp -R 3ai-bridge-main/. . && rm -rf 3ai-bridge-main main.zip && chmod +x 업데이트.command && open .
```

3. Finder 창이 열리면서 파일들이 보이면 완료 — 이 폴더가 설치 폴더입니다.
4. 크롬 `chrome://extensions` → 개발자 모드 → **"압축해제된 확장 프로그램을 로드합니다"**
   → `서류(Documents)/3ai-bridge` 폴더 선택

> 다음부터 업데이트할 때도 이 한 줄을 다시 붙여넣으면 됩니다.
> (또는 폴더 안의 `업데이트.command` 더블클릭 — 위 명령이 실행 권한을 붙여 두므로
> 그다음부터는 경고 없이 열립니다)

### 방법 B — 보안 설정에서 허용

1. 경고창에서 **완료** 클릭
2. **시스템 설정** → **개인정보 보호 및 보안** → 아래로 스크롤
3. "'업데이트.command'이(가) 차단되었습니다" 옆의 **그래도 열기** 클릭
4. 다시 파일을 더블클릭 → **열기** 확인

---

## 4-2. 바로가기로 카페 열기 (권장)

**아이콘 하나만 누르면** 크롬 실행부터 3대장 창 배치까지 한 번에 됩니다.

1. 설치 폴더 안의 **`카페열기.command`**(맥) 또는 **`카페열기.bat`**(윈도우)를
   찾습니다.
2. 바탕화면/Dock/작업표시줄에 올려 둡니다.
   - **맥**: 파일을 **Dock 오른쪽(휴지통 옆)** 으로 끌어다 놓기
   - **윈도우**: 파일 오른쪽 클릭 → **보내기 → 바탕 화면(바로 가기 만들기)**
3. 앞으로는 그 아이콘만 더블클릭하면 카페가 열립니다.
   크롬이 꺼져 있든 이미 켜져 있든 똑같이 동작하고, 별도 권한도 필요 없습니다.

> **크롬을 그냥 켤 때는 카페가 뜨지 않습니다.** 메일·검색 등 평소 용도로
> 크롬을 여는 데 방해가 되지 않도록, 카페는 바로가기(또는 `Alt`+`3`)로
> 부를 때만 열립니다.
>
> 크롬을 켤 때마다 자동으로 열리길 원하면 **설정 → 크롬 시작**에서
> "크롬을 켜면 자동으로 열기"를 켜세요(기본 꺼짐).

---

## 5. 업데이트 (ZIP 다시 받을 필요 없음)

확장이 새 버전으로 바뀌면, 설치 폴더 안의 업데이트 파일을 **더블클릭** 하세요.

- 윈도우: **`업데이트.bat`** 더블클릭
- 맥: **`업데이트.command`** 더블클릭
  (처음 한 번은 맥이 막을 수 있습니다 → 파일을 오른쪽 클릭 → **열기**)

최신 버전이 자동으로 내려받아져 폴더에 덮어써집니다. 그다음
`chrome://extensions` 에서 3대장 브리지 카드의 **새로고침(⟳) 버튼** 하나만
누르면 끝입니다.

## 6. 문제가 생겼을 때

| 증상 | 해결 |
|---|---|
| Alt+3 이 안 먹힘 | `chrome://extensions/shortcuts` 에서 단축키 확인 |
| "페이지와 통신 실패" | 해당 사이트 탭에서 **F5**(새로고침) 후 다시 전송 |
| "창이 열려 있지 않음" | 먼저 `Alt`+`3` 으로 창을 엽니다 |
| ❌ 가 계속 뜸 | 사이트 화면이 개편된 경우입니다. **[수리요청.md](수리요청.md)** 참고 |
| 확장이 사라짐 | 폴더를 옮기거나 지웠는지 확인 후 2단계 재실행 |

---

## 7. 개인정보 안내

- 질문 내용은 **여러분의 브라우저 밖으로 나가지 않습니다.**
- 외부 서버, 데이터베이스, API 키를 전혀 쓰지 않습니다.
- 사용 기록 수집(애널리틱스)이 없습니다.
- 설정과 마지막 질문만 크롬 로컬 저장소에 보관됩니다.
