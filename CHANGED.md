# 변경 파일 목록 — 기능 변경이 없음을 증명하는 문서

브랜치 `diagnose/gemini-truncation` · 기준 커밋 `64e47fa`

원칙: **관찰만 추가, 동작은 그대로.** 아래 표의 모든 항목은
(1) 새 파일이거나, (2) 기존 로직 사이에 끼워 넣은 로깅 호출이거나,
(3) 기본값이 현재 동작과 동일한 설정 항목입니다.
리팩터링·정리·포매팅 변경은 하지 않았습니다.

| 파일 | 변경 | 이유 | 동작 영향 |
|---|---|---|---|
| `diag.js` | **신규** | 계측 모듈(동기 캡처 + 비동기 해시 큐 + 링버퍼 저장) | 없음 — 아무도 호출하지 않으면 아무 일도 안 함 |
| `tools/analyze-logs.mjs` | **신규** | 로그 자동 판정기 (Node 전용, 확장과 무관) | 없음 |
| `DIAGNOSIS.md` | **신규** | Q1~Q10 답변과 근거 | 없음 |
| `CHANGED.md` | **신규** | 이 문서 | 없음 |
| `manifest.json` | content_scripts 목록에 `diag.js` 한 줄 추가 | 탭에서 계측 모듈을 쓰기 위해 | 없음 — 파일 추가일 뿐, 실행 순서·권한 불변 |
| `background.js` | ① `importScripts` 에 `diag.js` 추가 ② 재주입 `files` 목록에 `diag.js` 추가 ③ `진단로그` 메시지 핸들러 추가(신규 분기) ④ `사이트에전송`에 6번째 인수 `진단` 추가(로그 꼬리표만 전달) ⑤ `대상` 계산에 `주입순서` 분기 추가 | 계측 배선 · 순서 실험 스위치 | 없음 — ③은 새 종류의 메시지라 기존 분기에 닿지 않고, ④는 `질문전송` 메시지에 `sendId`/`position` 필드만 얹으며 `content.js`는 이 값을 로그에만 씀. ⑤는 `주입순서`가 빈 배열(기본값)이면 기존과 완전히 동일한 `설정.창순서` 사용 |
| `content.js` | ① 상단에 `진단기록` 안전판(모듈 없으면 무시) ② `진단원문읽기`(다듬지 않는 읽기) ③ `진단말풍선캡처`(D 단계) ④ 주입 직전 B 기록 ⑤ 전송 직전 C 기록 ⑥ 전송 성공 뒤 D 캡처 예약 | 4개 지점 계측 | 없음 — ④⑤는 `await` 없는 동기 호출, ⑥은 `await` 없이 떼어 낸 비동기 작업(응답을 지연시키지 않음). 모두 try/catch |
| `selectors.js` | 세 사이트에 `사용자말풍선`, `펼치기버튼` 항목 추가 | D 단계에서 내 말풍선을 읽기 위해 | 없음 — 기존 코드가 읽지 않는 새 키 |
| `popup.html` | ① `진단 로그` 버튼 추가 ② `diag.js` 스크립트 추가 | 로그 내보내기 | 없음 — 새 버튼, 기존 버튼 미변경 |
| `popup.js` | ① 전송 맨 앞에서 A 기록 ② `동시질문` 메시지에 `sendId` 필드 추가 ③ 전송 뒤 `진단요약출력` 호출(6초 뒤 console.table) ④ 내보내기 버튼 핸들러 | 계측·내보내기 | 없음 — ①은 `질문` 계산 뒤·`버튼.disabled` 앞의 동기 호출, ③은 `setTimeout`으로 완전히 분리 |
| `config.js` | `주입순서: []` 기본값 추가 | 순서 실험 스위치 | 없음 — 빈 배열이면 기존 경로 |
| `options.html` / `options.js` | `주입순서` 선택 항목 추가·저장·복원 | 위와 동일 | 없음 — 기본 선택지가 "기본 (창 순서 그대로)" |

## 하지 않은 것 (금지 사항 확인)

- ❌ 주입 방식 변경 — `글자넣기`, `execCommand` 호출, 대기 시간(150ms/250ms/
  12회·120회), `전송하기` 로직 **한 글자도 바꾸지 않음**
- ❌ 전송 전 검증 게이트 — 길이·해시 비교로 전송을 막는 코드 없음
- ❌ 리팩터링 / 코드 정리 / 포매팅 변경 — 기존 함수 본문 재배열 없음
- ❌ 로깅으로 인한 지연·`await` 추가 — 주입·전송 경로에서 진단 관련
  `await` 는 0건 (해시는 `setTimeout(...,0)` 큐, D 단계는 떼어 낸 작업)
- ❌ 원인 추정에 따른 선제적 수정 — `DIAGNOSIS.md` 3절의 가설은 보고만 함
- ❌ 버전 번호 변경 — `manifest.json` 은 `1.17.1` 그대로(진단 브랜치이므로)

## 검증

- `node --check` 통과: `diag.js`, `background.js`, `content.js`, `popup.js`,
  `config.js`, `options.js`
- Playwright(헤드리스)로 확장 적재 후 실측:
  - 명령바·설정 페이지 JS 오류 **0건**
  - 2001자 원문 전송 시 A 단계 레코드 1건 저장 확인
    (`length 2001 / byteLength 5003 / sha256·sha256Norm 생성됨 / site "controller"`)
  - `진단 로그` 버튼 존재, 설정의 `주입순서` 기본값 `""`(=기존 동작)
- `tools/analyze-logs.mjs` 를 합성 로그(제비만 D에서 1000자로 잘림)로 시험 →
  `❌ 전송/상태 동기화 문제 (C ≠ D)` 및 교차표 정상 출력
